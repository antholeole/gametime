mutation Authenticate($token: String!, $auth_provider: AuthProvider!) {
  authenticate(
    token: $token,
    auth_provider: $auth_provider
    ) {
		access_token,
    admin,
    id,
    refresh_token
  }
}