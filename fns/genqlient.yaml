# Default genqlient config; for full documentation see:
# https://github.com/Khan/genqlient/blob/main/docs/genqlient.yaml
schema: schema.graphql
operations:
- "queries/*.graphql"
generated: queries/gql_queries.go
bindings:
  uuid:
    type: string